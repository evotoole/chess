
'''
class Pawn:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos
    def two_forward(chessBoard):
        #MAKE CLASS TO CHECK IF MOVES ARE LEGAL

    def one_forward(chessBoard):
    #EN PASSANT

    def take(chessBoard):



class Knight:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos
   
  

    #FRONT LEFT
    def move_FL(chessBoard):
    
    #FRONT RIGHT
    def move_FR(chessBoard):

    #LEFT UP
    def move_LU(chessBoard):

    #LEFT BACK
    def move_LB(chessBoard):

    #BACK LEFT
    def move_BL(chessBoard):

    #BACK RIGHT
    def move_BR(chessBoard):

    #RIGHT UP
    def move_RU(chessBoard):

    #RIGHT BACK
    def move_RB(chessBoard):


class Bishop:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos

    #UP RIGHT
    def move_UR(chessBoard, num_squares):

    #UP LEFT
    def move_UL(chessBoard, num_squares):

    #BACK RIGHT
    def move_BR(chessBoard, num_squares):

    #BACK LEFT
    def move_BL(chessBoard, num_squares):
   

class Rook:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos
    #BLACK OR WHITE
    #ALL MOVES (NUMBER OF SQUARES IN DIRECTION)

class King:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos
    #BLACK OR WHITE
    #CHECK, CHECKMATE
    #ALL 8 MOVES

class Queen:
    def __init__(self, colour, current_pos, next_pos = None):
        self.colour = colour
        self.current_pos = current_pos
        if next_pos:
            self.next_pos = next_pos
        else:
            self.next_pos = current_pos
    #BLACK OR WHITE
    #ALL MOVES NUMBER OF SQUARES IN DIRECTION)

'''